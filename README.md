# speechTT
This is a python program to convert speech into text using Speech Recognition.

# install the following:- 
``` pip install SpeechRecognition```

```pip install PyAudio```

To run the program you have to paste the **.wav** format speech file into same directory and make sure you have active internet connection then run the following command in terminal `python filename.py`.
